import { SortServersPipe } from './sort-servers.pipe';

describe('SortServersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortServersPipe();
    expect(pipe).toBeTruthy();
  });
});
